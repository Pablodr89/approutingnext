import Carrito from '@/components/Carrito';

export const metadata = {
	title: 'Guitar - Carrito',
	description: 'Carrito Tienda',
};

const page = () => {
	return (
		<>
			<Carrito />
		</>
	);
};
export default page;
